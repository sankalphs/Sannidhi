You are working in D:\Coding\Sannidhi (Next.js 15 + Tailwind v4 + Convex, Windows, pwsh). Make ONLY the edits described below. Do NOT run typecheck, lint, build, or tests — the parent agent verifies centrally afterward. Do NOT touch any files other than the ones listed. Write code in the existing style (no comments except where explicitly specified).

## Edit 1: src/app/(faculty)/faculty/sessions/[sessionId]/live-board.tsx
Make the roster table header sticky within a vertically-scrollable wrapper:
- Change the table wrapper div (currently `<div className="-mx-6 overflow-x-auto px-6">`) to `<div className="-mx-6 max-h-[70vh] overflow-auto px-6">`.
- In `<thead>`: remove `border-b` from the `<tr className="text-muted-foreground border-b text-left text-xs font-medium">` (leaving `text-muted-foreground text-left text-xs font-medium`), and instead give EVERY `<th scope="col">` the classes `border-border/70 sticky top-0 z-10 border-b bg-card` in addition to its existing padding classes. The first four th have `pr-4 pb-2`, the last has `pb-2` only — keep those, just add the sticky classes.

## Edit 2: src/app/(faculty)/faculty/sessions/[sessionId]/qr-panel.tsx
Line ~181 currently:
```tsx
<img src={dataUrl} alt="Session QR code" width={280} height={280} />
```
Replace with:
```tsx
{/* Data URL source: next/image cannot optimize inline data URLs. */}
{/* eslint-disable-next-line @next/next/no-img-element */}
<img src={dataUrl} alt="Session QR code" width={280} height={280} />
```

## Edit 3: NEW FILE src/components/shell/mobile-nav.tsx
Create exactly this client component (it replaces a details/summary nav whose aria-expanded state screen readers can't track):
```tsx
"use client";

import { Menu, X } from "lucide-react";
import { useState } from "react";

import { SideNav } from "@/components/shell/side-nav";
import { SignOutButton } from "@/components/shell/sign-out-button";
import type { NavItem } from "@/lib/auth/nav";

export function MobileNav({
  roleLabel,
  nav,
}: {
  roleLabel: string;
  nav: NavItem[];
}) {
  const [open, setOpen] = useState(false);

  return (
    <>
      <button
        type="button"
        aria-expanded={open}
        aria-controls="mobile-nav-menu"
        aria-label={open ? "Close navigation menu" : "Open navigation menu"}
        onClick={() => setOpen((value) => !value)}
        className="hover:bg-accent focus-visible:ring-ring/50 -ml-2 flex size-9 items-center justify-center rounded-md transition-colors outline-none focus-visible:ring-[3px]"
      >
        {open ? <X className="size-4" /> : <Menu className="size-4" />}
      </button>
      {open ? (
        <div
          id="mobile-nav-menu"
          className="bg-background absolute top-11 left-0 z-50 min-w-56 rounded-lg border p-2 shadow-lg"
        >
          <p className="text-muted-foreground px-3 pb-2 font-mono text-[10px] tracking-[0.16em] uppercase">
            {roleLabel} panel
          </p>
          <SideNav nav={nav} variant="paper" onNavigate={() => setOpen(false)} />
          <div className="border-border mt-2 border-t pt-2">
            <SignOutButton />
          </div>
        </div>
      ) : null}
    </>
  );
}
```

## Edit 4: src/components/shell/side-nav.tsx
Add an optional `onNavigate` callback invoked when a nav link is clicked:
- Props type becomes:
```tsx
type SideNavProps = {
  nav: NavItem[];
  className?: string;
  variant?: "board" | "paper";
  onNavigate?: () => void;
};
```
- Destructure `onNavigate` in the component signature.
- On the `<Link ...>` inside the map, add `onClick={onNavigate}` (keep everything else, including `aria-current`).

## Edit 5: src/components/shell/app-shell.tsx
- Add import: `import { MobileNav } from "@/components/shell/mobile-nav";` (group with the other `@/components/shell` imports, alphabetically after locked-empty-state import if present; current imports are Seal/Wordmark from marketing/wordmark, SignOutButton, SideNav — place MobileNav import so the list stays alphabetically sorted: mobile-nav comes after marketing/wordmark and before sign-out-button).
- Delete the entire `<details className="group relative">...</details>` block (the hamburger summary + dropdown, roughly lines 47-67) and replace it with `<MobileNav roleLabel={roleLabel(role)} nav={nav} />`. The dropdown previously positioned absolute inside the details; the header is `sticky` so absolute positioning still anchors to the header — no wrapper changes needed.
- Change `text-primary-foreground/50` in the sidebar "panel" label paragraph (line ~32) to `text-primary-foreground/65` (contrast fix on the chalkboard sidebar).
- Keep the SignOutButton import (still used by the desktop sidebar).

## Edit 6: src/app/(student)/student/check-in/check-in-panel.tsx
Distinguish "browser can't scan QR" from "camera unavailable" copy:
- Add below the existing `type CameraState` declaration:
```tsx
type CameraUnavailableReason = "scanner_unsupported" | "camera_unavailable";
```
- Add state next to `cameraState`: `const [unavailableReason, setUnavailableReason] = useState<CameraUnavailableReason | null>(null);`
- In the scanner effect, where it currently does:
```tsx
if (!DetectorCtor || !navigator.mediaDevices) {
  setCameraState("unavailable");
  return;
}
```
also add `setUnavailableReason("scanner_unsupported");` before the return.
- In the `.catch(() => { if (!cancelled) setCameraState("unavailable"); })` at the end of the getUserMedia chain, also call `setUnavailableReason("camera_unsupported" === "x" ? "camera_unavailable" : "camera_unavailable")` — NO, simply: change that catch to:
```tsx
.catch(() => {
  if (!cancelled) {
    setUnavailableReason("camera_unavailable");
    setCameraState("unavailable");
  }
});
```
- Replace the unavailable message JSX:
```tsx
{cameraState === "unavailable" ? (
  <p className="text-muted-foreground rounded-lg border border-dashed p-4 text-sm">
    Camera is unavailable on this device — paste the check-in code instead.
  </p>
) : (
```
with:
```tsx
{cameraState === "unavailable" ? (
  <p className="text-muted-foreground rounded-lg border border-dashed p-4 text-sm">
    {unavailableReason === "scanner_unsupported"
      ? "QR scanning isn't supported in this browser — paste the check-in code below instead."
      : "Camera is unavailable on this device — paste the check-in code below instead."}
  </p>
) : (
```

## Edit 7: src/components/marketing/live-session-demo.tsx
Stop screen readers from hearing the churning decorative animation, without losing the static description:
- Change the QR svg's `aria-label={`Rotating session challenge QR, current token ${token}`}` to the static `aria-label="Animated preview of a rotating session QR code"`.
- Add `aria-hidden="true"` to the div `<div className="flex w-full flex-col items-center gap-1.5" data-testid="demo-qr-token">` (the token text + countdown bar churn every 8s).
- Add `aria-hidden="true"` to the feed `<ul className="flex flex-col">` (rows append every 2.6s; the "Verification board" label right above stays visible to assistive tech).

Report back: list of files changed with a one-line summary each, and anything you could not complete.