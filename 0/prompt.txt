Implementation task in D:\Coding\Sannidhi (branch feat/phase-5-audit-resilience, already checked out). Stack: Next.js + Convex, bun runtime, TypeScript strict. Write code following existing file conventions exactly. DO NOT commit — leave changes in working tree and report when done.

CONTEXT: This repo is building Phase 5 "audit-resilience". The event ledger hash chain already exists: `convex/ledger.ts` has `appendLedgerEvent` (internalMutation, per-institution seq via `by_institution_seq`) + `verifyChain` query; `convex/lib/attendance_event.ts` has `appendAttendanceEvent` (plain async fn on MutationCtx — THE chokepoint every attendance writer funnels through); pure hashing in `src/lib/ledger/hash.ts` (`computeEventHash`, canonical JSON, SHA-256). Two independent chains exist: `event_ledger` (institution-wide audit) and `attendance_events` (per-student decision records). Read these three files first, plus `convex/schema.ts`, `convex/crons.ts`, `convex/maintenance.ts`, `convex/classSessions.ts`.

YOUR SCOPE (foundation layer — two other agents will build corrections workflow and offline sync on top of your seam):

1. NEW pure module `src/lib/attendance/lifecycle.ts`:
   - Export `ATTENDANCE_STATES` (const tuple): "initiated" | "authenticated" | "session_verified" | "presence_evaluated" | "risk_evaluated" | "step_up" | "verified" | "flagged" | "rejected" | "corrected", and type `AttendanceEventState`.
   - Export `CORRECTIONABLE_STATES = ["verified","flagged","rejected"]` (terminal decision states that a correction may overwrite).
   - Export transition helpers used by the seam (pure, unit-testable): something like `isStartableState(state)` (every state except "corrected" may begin an attempt entry — intermediates exist for future progressive writers) and `isValidCorrection(state, correctsEventId)` (state==="corrected" iff correctsEventId is present; non-corrected events must NOT carry correctsEventId). Also export a documented linear progression order for the intermediate pipeline (initiated→authenticated→session_verified→presence_evaluated→risk_evaluated→decision states) as `canFollowInPipeline(prev,next)` for future writers — keep it simple data, not machinery.
2. Extend `appendAttendanceEvent` in `convex/lib/attendance_event.ts`:
   - Accept full `state: AttendanceEventState`, `origin: "online"|"offline-faculty"|"mobile"` (default "online"), optional `correctsEventId?: Id<"attendance_events">`.
   - Enforce the lifecycle rules from (1) — throw ConvexError on violation.
   - SERVER-TIME AUTHORITY: the seam stamps `capturedAt: Date.now()` itself; remove capturedAt from caller args. Update ALL existing callers (grep `appendAttendanceEvent(` across convex/ — checkin.ts, challenges.ts, classSessions.ts) accordingly.
   - PER-INSTITUTION CHAIN: today it chains via global `by_seq` index; switch to per-institution sequencing like appendLedgerEvent. Add schema index `by_institution_seq` on attendance_events; remove `by_seq` index ONLY if nothing else references it (grep first — if referenced, leave the index and just stop using it here).
   - When correctsEventId is set, validate the target event exists, its state ∈ CORRECTIONABLE_STATES, and it has not itself been corrected (query `by_corrects_event` index for an existing corrected event referencing it).
   - Include origin/correctsEventId in the hashed payload consistently (payload already embeds state/origin — extend with correctsEventId when present).
3. Session lifecycle audit events: `pause`, `restart`, `close` in `convex/classSessions.ts` and the auto-close path in `convex/maintenance.ts` currently write ZERO ledger events. Emit event_ledger entries (category "attendance", types "session.paused" / "session.restarted" / "session.closed" / "session.auto_closed") via `ctx.runMutation(internal.ledger.appendLedgerEvent, ...)` with actorUserId (where actor exists; auto-close has none) and payload {sessionId, sectionId}.
4. Retention cron (inside the Event Ledger per plan):
   - Env knob `RETENTION_DAYS` parsed with the existing `parseTtlMs`-style tolerance (follow how other TTL env vars in this repo are read; default 730 days).
   - New internalMutation (suggest placing in `convex/maintenance.ts` alongside existing sweeps): batched deletion of `event_ledger` AND `attendance_events` rows with createdAt/capturedAt older than cutoff, capped per run (e.g. ≤200 rows per table) so one run can't blow limits. Register a daily cron in `convex/crons.ts`.
5. `verifyChain` anchoring fix in `convex/ledger.ts`: once retention deletes old rows, verifying from seq 0 returns invalid because genesis is gone. Change semantics: when the requested window's predecessor is missing BUT the window starts at the OLDEST SURVIVING event, treat that event as a fresh anchor (valid) and return `anchoredAtSeq: <seq>` in the result. A genuinely broken link mid-window still returns invalid. Apply the same anchoring to the new query in (6).
6. New `verifyAttendanceChain` query in `convex/ledger.ts`: mirrors `verifyChain` but walks the `attendance_events` per-institution chain recomputing hashes with the same LedgerHashInput shape the writer used (type "attendance.session_checkin", subjectUserId=studentId, payload matching what appendAttendanceEvent hashes — keep the two computations in agreement by sharing any payload-shaping helper between writer and verifier rather than duplicating it). Admin/auditor gated like verifyChain. If `src/app/(audit)/audit/events/page.tsx` surfaces verifyChain results with a button/query, wire the attendance-chain twin in minimally beside it; if surfacing is nontrivial, skip UI and just export the query.
7. SCHEMA EDITS (you own ALL phase-5 schema edits; other agents were told not to touch schema.ts):
   - attendance_requests: add optional `sessionId?: v.id("class_sessions")`, `eventId?: v.id("attendance_events")`, `reviewedByUserId?: v.id("users")`, `decision?: v.union(v.literal("approved"), v.literal("dismissed"))`, and extend status union to "submitted"|"approved"|"dismissed" (existing rows are all "submitted"; nothing sets "reviewed").
   - class_sessions: add optional `offlineKeyHash?: v.string()`.
   - attendance_events: add optional `syncNonceHash?: v.string()` + index `by_nonce_hash`; add `by_institution_seq` index per (2).
8. TESTS: new vitest suite `tests/attendance-lifecycle.test.ts` (`// @vitest-environment node` like tests/ledger-hash.test.ts) covering the pure lifecycle module thoroughly. Run it scoped.

VERIFY before reporting: `bun run typecheck` green, `bunx eslint` on touched files clean (match repo lint config), `bunx vitest run tests/attendance-lifecycle.test.ts tests/ledger-hash.test.ts` green. Do NOT run the dev build or e2e.

REPORT BACK: list of files changed with a 1-line summary each, exact exported symbol names + signatures you added/changed (especially appendAttendanceEvent's new arg shape and verifyChain/verifyAttendanceChain return shapes), any deviations from the above with reasons, and the outputs of typecheck/lint/tests.